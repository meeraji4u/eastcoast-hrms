# ── App ──────────────────────────────────────────────────────────
SECRET_KEY=change-this-to-a-long-random-string-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=480

# ── eSSL MS SQL Server (your existing biometric DB) ────────────────
ESSL_SERVER=192.168.1.23
ESSL_PORT=1433
ESSL_DATABASE=iclock
ESSL_USERNAME=sa
ESSL_PASSWORD=your_sa_password_here

# ── SMTP (for OTP emails) ───────────────────────────────────────────
# Gmail example: use an App Password, not your real Gmail password.
# Create one at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=hrms@eastcoasthospitals.in
SMTP_PASSWORD=your_app_password_here
SMTP_FROM_NAME=EastCoast HRMS
